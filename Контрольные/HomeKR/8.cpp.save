#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {

    int n = 0, m = 0;
    int i, j, N;

    scanf("%d%d", &n, &m);

    N = n * m;

    int * matrix = (int *) malloc(N*sizeof(int));

    for (i = 0; i < n; i++) {
        for (j = 0; j < m; j++) {
            scanf("%d", &matrix[i*m+j]);
        }
    }

    int * matrix = (int *)malloc(N*sizeof(int));

    for (i = 0; i < m; i++) {
        for(j = 0; j < n; j++){
            pT[i*n + (n - j -1)] = p[j*m + i];
        }
    }

    for (i = 0; i < m; i++) {
    for(j = 0; j < n; j++){
    printf("%d ", pT[i*n + j]);
    }
    printf("\n");
    }

    free(p);
    free(pT);
}
